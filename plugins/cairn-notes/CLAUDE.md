# Athena Notes

Claude Code: see `@AGENTS.md` for the agent framework and conventions.

This plugin ships via Claude Code's plugin mechanism. User identity is managed separately at `~/.claude/cairn/identity.md` — run `/cairn-setup` to populate it.
